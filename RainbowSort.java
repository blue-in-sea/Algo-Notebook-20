public class RainbowSort {

    public int[] rainbowSort(int[] array) {
        if (array == null || ) {
            
        }
        
        return array;
    }
}


// Bucket Sort used in 大量重复的情况
